#include <iostream>
#include <iomanip>
#include <string>
#include <fstream>
#include <ctime>

#include <vector>

using namespace std;

void clearScreen();
char displayMenu();
int random2or4();
int random_index(int size);